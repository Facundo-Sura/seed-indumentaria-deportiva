const { Router } = require("express");

const router = Router();

router.get("/"); // Get all questions
router.post("/"); // Create a new question
router.put("/"); // Update a question
router.patch("/"); // Partially update a question
router.delete("/"); // Delete a question
