const {Router} = require('express');

const router = Router();

router.get('/'); // Get all reviews
router.post('/'); // Create a new review
router.put('/'); // Update a review
router.patch('/'); // Partially update a review
router.delete('/'); // Delete a review